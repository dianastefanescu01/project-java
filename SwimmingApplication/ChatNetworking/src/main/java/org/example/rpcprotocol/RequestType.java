package org.example.rpcprotocol;

public enum RequestType {
    LOGIN,
    LOGOUT,
    REGISTER,
    ADD_REGISTRATION,
    ADD_PARTICIPANT,
    GET_RACES,
    GET_PARTICIPANTS,
    GET_REGISTRATIONS,
    GET_LOGGED_USERS
}
